name: Continuous Stuff
on:
  push:
    branches:
      - feature/*
      - hotfix/*
      - master
    tags:
      - v*
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Dotnet build
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Build
        run: dotnet build -c Release --nologo
      - name: Test
        run: dotnet test -c Release --no-build --nologo
      - name: Push
        run: |
          sed 's/GITHUB_TOKEN/${{ secrets.GITHUB_TOKEN }}/g' .nuget.config > nuget.config
          dotnet nuget push "src/Slack.Webhooks/bin/Release/*.nupkg" --source "github"
env:
  DOTNET_CLI_TELEMETRY_OPTOUT: 1